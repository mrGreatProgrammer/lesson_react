import React from "react";
import { ChevronLeft, ChevronRight } from "../../imgs/icons";
// import ChevronLeft from "../../imgs/icons/ChevronLeft/ChevronLeft";
// import ChevronRight from "../../imgs/icons/ChevronRight/ChevronRight";
// import { useGetPostsByLimitQuery } from "../../services/posts";
// import { useGetPokemonByNameQuery } from "../../services/app";

const List = () => {
    

  return (
    <div>
      <ul>
        {/* {error ? (
          <>Oh no, there was an error</>
        ) : isFetching ? (
          <>Loading...</>
        ) : (
          data.map((e, i) => <li key={i}>{e.title}</li>)
        )} */}
      </ul>

      
    </div>
  );
};

export default List;
